package com.example.ui.port

import android.os.Bundle
import android.os.PersistableBundle
import androidx.ui.engine.platform.io.app.FlutterActivity
import androidx.ui.engine.platform.io.plugin.common.PluginRegistry

/**
 * A simple "hello world" Activity to run crane on screen.
 */
class MainActivity : FlutterActivity() {

    override fun onCreate(savedInstanceState: Bundle?, persistentState: PersistableBundle?) {
        super.onCreate(savedInstanceState, persistentState)
        GeneratedPluginRegistrant.registerWith(this)
    }

    fun runApp() {
        // TODO(Migration/Filip): Run the app here after the Activity gets created
    }
}

/**
 * Copied from the "hello world" example, this file was originally autogenerated.
 */
object GeneratedPluginRegistrant {
    fun registerWith(registry: PluginRegistry) {
        if (alreadyRegisteredWith(registry)) {
            return
        }
    }

    private fun alreadyRegisteredWith(registry: PluginRegistry): Boolean {
        val key = GeneratedPluginRegistrant::class.java.canonicalName
        if (registry.hasPlugin(key)) {
            return true
        }
        registry.registrarFor(key)
        return false
    }
}