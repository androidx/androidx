import static androidx.build.dependencies.DependenciesKt.KOTLIN_R4A_STDLIB

plugins {
    id("AndroidXPlugin")
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    defaultConfig {
        minSdkVersion 21
    }
}

dependencies {
    implementation(project(":ui-port"))
    implementation(project(":ui-r4a"))
    api(KOTLIN_R4A_STDLIB)
}
