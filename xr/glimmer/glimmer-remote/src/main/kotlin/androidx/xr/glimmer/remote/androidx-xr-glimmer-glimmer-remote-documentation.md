# Module root

Glimmer is the UI library for augmented Android XR experiences

# Package androidx.xr.glimmer.remote

Provides Glimmer design system components through a declarative, Compose-like API to generate
`Remote Compose` (`.rc`) documents.