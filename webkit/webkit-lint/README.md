# WebKit Lint

This directory contains custom lint rules for the `androidx.webkit` library.
These lint rules are for developers that use this AndroidX library.
It exists to catch common errors and follow best practices when using WebKit APIs,
such as ensuring `onRenderProcessGone` is handled properly to avoid application crashes.
