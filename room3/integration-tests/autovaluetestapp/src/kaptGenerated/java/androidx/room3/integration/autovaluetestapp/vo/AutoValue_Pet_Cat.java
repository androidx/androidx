

package androidx.room3.integration.autovaluetestapp.vo;

import androidx.room3.ColumnInfo;
import androidx.room3.PrimaryKey;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_Pet_Cat extends Pet.Cat {

  @PrimaryKey(autoGenerate = true)
  private final long petId;

  @PrimaryKey(autoGenerate = true)
  private final long ownerId;

  @ColumnInfo(name = "kittyName")
  private final String name;

  AutoValue_Pet_Cat(
      long petId,
      long ownerId,
      String name) {
    this.petId = petId;
    this.ownerId = ownerId;
    if (name == null) {
      throw new NullPointerException("Null name");
    }
    this.name = name;
  }

  @PrimaryKey(autoGenerate = true)
  @Override
  public long getPetId() {
    return petId;
  }

  @PrimaryKey(autoGenerate = true)
  @Override
  public long getOwnerId() {
    return ownerId;
  }

  @ColumnInfo(name = "kittyName")
  @Override
  public String getName() {
    return name;
  }

  @Override
  public String toString() {
    return "Cat{"
         + "petId=" + petId + ", "
         + "ownerId=" + ownerId + ", "
         + "name=" + name
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof Pet.Cat) {
      Pet.Cat that = (Pet.Cat) o;
      return this.petId == that.getPetId()
          && this.ownerId == that.getOwnerId()
          && this.name.equals(that.getName());
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= (int) ((petId >>> 32) ^ petId);
    h$ *= 1000003;
    h$ ^= (int) ((ownerId >>> 32) ^ ownerId);
    h$ *= 1000003;
    h$ ^= name.hashCode();
    return h$;
  }

}
