

package androidx.room3.integration.autovaluetestapp.vo;

import androidx.room3.Ignore;
import androidx.room3.PrimaryKey;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_Person extends Person {

  @PrimaryKey(autoGenerate = true)
  private final long id;

  private final String firstName;

  private final String lastName;

  @Ignore
  private final boolean coolPerson;

  private AutoValue_Person(
      long id,
      String firstName,
      String lastName,
      boolean coolPerson) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.coolPerson = coolPerson;
  }

  @PrimaryKey(autoGenerate = true)
  @Override
  public long getId() {
    return id;
  }

  @Override
  public String getFirstName() {
    return firstName;
  }

  @Override
  public String getLastName() {
    return lastName;
  }

  @Ignore
  @Override
  public boolean isCoolPerson() {
    return coolPerson;
  }

  @Override
  public String toString() {
    return "Person{"
         + "id=" + id + ", "
         + "firstName=" + firstName + ", "
         + "lastName=" + lastName + ", "
         + "coolPerson=" + coolPerson
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof Person) {
      Person that = (Person) o;
      return this.id == that.getId()
          && this.firstName.equals(that.getFirstName())
          && this.lastName.equals(that.getLastName())
          && this.coolPerson == that.isCoolPerson();
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= (int) ((id >>> 32) ^ id);
    h$ *= 1000003;
    h$ ^= firstName.hashCode();
    h$ *= 1000003;
    h$ ^= lastName.hashCode();
    h$ *= 1000003;
    h$ ^= coolPerson ? 1231 : 1237;
    return h$;
  }

  static final class Builder extends Person.Builder {
    private Long id;
    private String firstName;
    private String lastName;
    private Boolean coolPerson;
    Builder() {
    }
    @Override
    Person.Builder id(long id) {
      this.id = id;
      return this;
    }
    @Override
    Person.Builder firstName(String firstName) {
      if (firstName == null) {
        throw new NullPointerException("Null firstName");
      }
      this.firstName = firstName;
      return this;
    }
    @Override
    Person.Builder lastName(String lastName) {
      if (lastName == null) {
        throw new NullPointerException("Null lastName");
      }
      this.lastName = lastName;
      return this;
    }
    @Override
    Person.Builder coolPerson(boolean coolPerson) {
      this.coolPerson = coolPerson;
      return this;
    }
    @Override
    Person build() {
      String missing = "";
      if (this.id == null) {
        missing += " id";
      }
      if (this.firstName == null) {
        missing += " firstName";
      }
      if (this.lastName == null) {
        missing += " lastName";
      }
      if (this.coolPerson == null) {
        missing += " coolPerson";
      }
      if (!missing.isEmpty()) {
        throw new IllegalStateException("Missing required properties:" + missing);
      }
      return new AutoValue_Person(
          this.id,
          this.firstName,
          this.lastName,
          this.coolPerson);
    }
  }

}
