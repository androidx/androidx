# Module root

androidx.appstate appstate

# Package appstate

Allows for the hoisting of Compose state beyond any UI (i.e. outside of an Android Activity) so that
it can be shared across different components within an application's context.