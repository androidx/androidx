# Ink Brush Compose Module

Ink's `brush` module with extensions specific to Jetpack Compose. Separate from
the main `brush` module so that clients not using Compose can avoid the Compose
dependency.
