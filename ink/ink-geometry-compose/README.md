# Ink Geometry Compose Module

Ink's `geometry` module with extensions specific to Jetpack Compose. Separate
from the main `geometry` module so that clients not using Compose can avoid the
Compose dependency.
