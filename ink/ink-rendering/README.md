# Ink Rendering Module

This module provides logic for rendering freehand strokes constructed with the
`strokes` module. Currently, there is just an `android` submodule which uses
Android platform rendering APIs.
